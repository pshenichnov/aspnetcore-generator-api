version: '3.1'

services:
  generator:
    image: my-registry:55000/aspnetcore/generator:ci-${BUILD_NUMBER}
    #ports:
    #  - "8080:80"
  mail:
    image: mailhog/mailhog
    #ports:
    #  - "8025:8025"
  integration:
    image: my-registry:55000/aspnetcore/generator:integration-${BUILD_NUMBER}
    build: .
